git checkout parsa -- path/to/your-file.js

git fetch
to search for it `git branch -r | grep "part-of-branch-name"`
git switch `branch/name` eg: `git switch feature/expertise`


delete a branch: git branch -D BRANCH
modify a branch name: git branch -m OLDBRAMCHNAME NEWBRANCHNAME
